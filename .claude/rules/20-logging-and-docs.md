# 20 - Logging and Docs (Local)

- Update relevant dev docs in the same change when behavior changes.
- If `dev-docs/` exists locally, keep a single source of truth per topic and link it from `dev-docs/README.md`.
