# Project Claude Configuration

This folder contains Claude configuration assets shared across the team.
Keep all binding rules in `AGENTS.md` and reference them here as needed.

Structure:
- `rules/` — project rules
- `commands/` — custom slash commands (optional)
- `skills/` — custom skills
